# ERD Notes — Team 2

## Relationships
- Store 1:N Product
- Category 1:N Product
- Product 1:1 Inventory

`stores.owner_user_id` به `users.user_id` در هسته DataPulse اشاره می‌کند. چون جدول `users` متعلق به زیرسیستم تیم 1 است، در اسکریپت تیم 2 این FK به‌صورت قابل فعال‌سازی ارائه شده و برای اجرای مستقل، ابتدا باید جدول `users` در دیتابیس اصلی وجود داشته باشد.

## 3NF
- هر جدول یک کلید اصلی دارد.
- ویژگی‌های هر رکورد به کلید همان جدول وابسته‌اند.
- اطلاعات فروشگاه، دسته‌بندی و محصول در جدول‌های جدا قرار گرفته‌اند.
- اطلاعات موجودی از محصول جدا شده است.
- `exchange_rate_to_irr` به‌عنوان نرخ تبدیلِ ثبت‌شده برای قیمت فعلی محصول در نظر گرفته شده؛ بنابراین مقدار آن بخشی از snapshot قیمت محصول است، نه یک جدول مستقلِ نرخ ارز.

## محدودیت‌های مهم
- قیمت و نرخ ارز منفی نیستند.
- موجودی و نقطه سفارش منفی نیستند.
- کد ارز سه حرفی است.
- نام فروشگاه و دسته‌بندی یکتا است.
- هر محصول فقط یک رکورد موجودی دارد.
